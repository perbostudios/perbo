# Handoff: round 8 of `home-loop-and-planning-care-package`

This file rides on the work-in-progress branch only. Delete it before anything here reaches the pull-request branch.

## Where things stand

- Pull request #13 (`home-loop-and-planning-care-package`) is `1e0de26` plus eight write-guard fix commits: `013d867`, `0b1b180`, `2256433`, `923a249`, `7e26a4b`, `e0f854b`, `c669777`, `350a097`. Its D-073 review at `350a097` approved. This branch carries none of the eight; the merge hazards are under "Merging with pull request #13" below.
- This branch holds `1e0de26` plus work-in-progress commits: six bundles built, reviewed and fixed; every fixer finished and reported. The round-8 review found no code blocker and two doc defects, which are being fixed. Every bundle has mutation-checked tests. The planning bundle (section 3) is fixed and independently reviewed, and that review's findings are fixed. The serial gate passes: install, build, runtime, corpus, validators (Python 3.11 with `yaml`), protected paths, the regression dry run, and every package's typecheck and lint. The test failures a root cloud container shows are its environment's and fail identically at `1e0de26`, which is green on GitHub: root ignores file modes and a set `GH_TOKEN` skips `gh auth status` (both pass as a non-root user without `GH_TOKEN`); `ssh-keygen` must be installed; `evaluation` `baseline.test.ts` and `runner` `loop/index.orphans.test.ts` fail there as any user, and `workspace` `diagnostic.ignored-paths.test.ts` times out only under the full parallel run. What remains: the founder's word to push the round's commit, and his local deploy with the profile migration: quit Perbo, then from the repository root `node scripts/migrate-workspace-round8.mjs ~/Library/Application\ Support/Perbo`. The second read of the Home, Watch-page and publish fixes is DONE and its findings are fixed (commit 6e77c31): Home and the review cards approved; the Watch page now carries whole turns with line breaks escaped on one physical line and no length cut; pull-request titles are whole sentences; every retained-publish refusal is tested and listed in D-NEW and `usage.ts`.
- Workflow the founder set: bundle by disjoint files → one Opus 5.5 builder per bundle → an independent Opus 5.5 reviewer per bundle (nothing lost, nothing beyond the ask, every test proven by mutation) → a fixer → a second read for large fixes → serial gate → ONE commit → local deploy on his Mac. No optimisation pass any more. No push or PR without his word. Commits carry `Assisted-by: LLM` and no `Co-Authored-By`. `packages/review/**` and `packages/runner/test/security.test.ts` are never edited: `git diff origin/main` on them is empty. `packages/model/**` is left as the green commit has it.

## Bundles and what is open

### 1. Continue after an interrupted run (runner `resume.ts`, `seal.ts`, `prompt.ts`, `loop/internal/{attempt,brief,state}.ts`, contracts `attempt.ts`)
Built and approved by two reviews. Retained diff taken with `--binary`; a diff that does not apply is dropped whole (reset to the sealed head) and the run continues; the diff is skipped where the branch already holds the sealed commit ("held"); the resumed executor's brief is truthful (`executor_resumed_v3`). Follow-ups noted, not fixed: a binary diff over 8 MiB leaves no `change.diff` and the refusal says "changed nothing"; the desktop's raw diff view shows base85 blocks.

### 2. Publish a retained branch later (`--publish-retained`; runner `loop/internal/retained.ts`, contracts `retained.ts`, CLI run command, desktop review/merge screens, D-NEW-publish-a-retained-branch-later)
Built, reviewed, fixed. "Next" always enabled on the review screen; the merge press publishes where no pull request exists. Declines now recorded at the seal (`ExecutionAttemptSchema.declines`); verification costs summed from `cv_` bundles; primary buttons at the bottom right; delivery record written under the run lock; preflight git and gh only. Fixed since: the `usage.ts` help lists every refusal; `retainedBranch` is read again under the run lock (`retained.ts:119`, CLI `run/index.ts:1431`); D-NEW's Built line records the known limit (a later run that declines without a new commit: the pull request lists the sealing run's declines). Second read done and its findings fixed (commit 6e77c31).

### 3. Planning flow for basic tickets and epics (renderer `planning/**`, `panes.ts` `flowFor`, `SimpleTask.tsx`, `ContractPane.tsx`, `ContractScreen.tsx`, `CriteriaEditor.tsx`, `GraphPane.tsx`, `GraphInspector.tsx`, `DriftPane.tsx`, `shared/{protocol,contract-editing}.ts`, host `plan/**`, `routes.ts` `graphEdit`/`replan`, both hosts, D-NEW-basic-and-epic-flows, D-128, D-130, `scripts/migrate-workspace-round8.mjs`)
DONE, then wave 2 of the founder's rulings built on it (uncommitted, awaiting review). As built: an archived stopped ticket whose run job left the journal lands on the stopped page; the chat's edits are marked (`EditingChange.by`), a hand edit marks nothing and clears them; a refused write says why; the last criterion cannot be deleted; `useDraftLanding` checks the impact of any opened basic planning with none recorded. An epic's criteria are edited by hand in the Graph node inspector (reword on blur, verification kind; the placeholder the Node button adds included), recorded as the person's and unmarked; only the epic's contract and the post-approval contract show the graph read-only. Rail order: Spec, (Graph), Explorer, Impact, contract, then Problems, lowest, only while a problem is open. A plan just drafted (Generate plan, Start over, Plan it again) is recorded as read (`read`) where the verdict admission wrote still holds for the spec's bytes (`draftedReading`), so its first confirm unchanged reads nothing; where that verdict is missing the draft is not recorded as read and its first confirm reads it. Nothing reads a drafted plan (wave 2 ruling): an epic lands on its Graph at once, with no wait; a basic ticket lands on Impact if flagged, else the contract, after a "Checking the impact" wait; neither lands on Problems. A fresh draft drops the problems of the plan it replaces (`reconcile`), a reading a Generate plan or Start over overtook lands nothing in either host (`redraftedSince`), and the Problems pane shows no wait on an arrival that reads nothing. At Confirm the plan (epic, via the Problems pane) and Confirm contract (basic) the plan is read only where `read` differs from the state (spec sections + promise texts), with a "Checking for drift" page while it runs; both hosts keep `form.draft` following hand graph edits so the state is the plan's own. Any open problem refuses the confirm for both shapes, with no way past. A reading happens only at a confirm; one that cannot run is retried (`untilItRuns`, `shared/reading-retry.ts`) and then reported by a centred pop-up (`ReadingFailedNotice`), which leaves nothing confirmed. Once all are resolved the tab goes and a person on it is moved back (epic: Graph; basic: contract).
Open for the founder:
- (c) Closed by the wave-2 review's fix (uncommitted): both hosts refuse a `run` that approves, in one sentence, while a planning over the ticket records problems open (`problemsHoldApproval` in `shared/contract-editing.ts`); the contract page holds its confirm for either shape whichever route reached it, and sends an epic whose plan moved since the last reading back through the Problems pane.
- (e) Person-read cuts: ruled and built under D-NEW-nothing-shown-is-cut (wave 2 and the founder's answers 1–8 below, uncommitted, awaiting review); its Not built line lists what is left: `@perbo/model`'s record of a transport's failure, its first line to 300 characters (SCP-188).
- (f) D-NEW-nothing-shown-is-cut states the rule.
- (g) Ruled and built (wave 2, uncommitted, awaiting review): `perbo drift --dismiss` and the host's `driftDismiss` stay, and no page offers them. A dismissal is refused, in one sentence, once `<KEY>.draft.json` records an edit by the person (`author: "you"`, changing the plan, not replaced by a re-draft) or where there is no snapshot; the chat's edits do not count. The desktop host passes the CLI's refusal on; the sample host refuses in the same words from its own `handEdited` record. Not covered: a contract file changed outside `perbo edit`, which approval's counter-seal refuses.
- (h) "Confirming contract" in the ruling is taken to be the existing "Approving the contract" wait (LoopScreen); the sample host approves instantly, so its flows assert the order against the loop page.
Closed as ruled (the founder's answers 9–11; current behaviour, nothing to build):
- "Checking the impact" stays: a basic ticket's fresh draft waits on it before landing on Impact or the contract.
- A plan whose admission verdict is missing is not recorded as read, so its first confirm gets a real reading.
- A contract edited outside `perbo edit` is not tracked: a dismissal reads only the draft's edit log, and approval's counter-seal refuses such a contract.
Follow-up (not now): a basic re-draft drops Explorer marks (predates this bundle; epics too).

### 4. Live Watch page (contracts `spoken.ts`, runner `adapter.ts`, `codex/index.ts`, `loop/internal/{review,seal}.ts`, desktop `runner-progress.ts`, `task-context.ts` `watchTranscript`, `retained-output.ts`, OutputScreen in `ReviewScreens.tsx`)
Built, reviewed, fixed (22 mutations caught): the recorded list covers every attempt of the run (`useOutputs`, `watchTranscript` over `{attempt, transcript}` pairs); Codex subagent lines marked `subagent: true` and skipped; `logTail` drops the first partial line at both cut sites; Codex command lines folded (`oneLine` in contracts `spoken.ts`); reviewer lines redacted through `openFindingLines` with tests; deterministic-source findings print as the runner's own `finding:` line; stage-word regression test; "Continue the task" at the far right of the stopped page. Second read done and its findings fixed (commit 6e77c31).

### 5. Home (`HomePage.tsx`, `ticket-workspace.ts`, `shared/archive.ts`, `Rail.tsx`, `TaskPage.tsx`, sample host)
Built, reviewed, fixed: `ticketOpened` re-sent when the ticket's row becomes unseen while open; "completed" = decided only, greens "waiting on your merge decision" (badge, header pill, greeting, filters); decided last under every sort; `aria-describedby` on the card; lifecycle rows moved to contracts `ticket-transitions.ts` (root + browser) and the sample host's `moveTicket`/`reopenTicket` walk only legal rows, a stop writes no row. Second read done and its findings fixed (commit 6e77c31).

### 6. Review cards (`ReviewScreens.tsx` cards, `styles.css`, docs/15:32)
Built and approved; the badge and table words are to "directly verified", "inferred", "evidence not retained", "not reviewed" (card and table view; table column "Established").

## Founder's rulings today (apply everywhere)
- No caps on the COUNT of records Perbo writes; only a count cap a decision states by number stays (D-094, D-117, D-127, D-128). Nothing a person reads is cut: D-NEW-nothing-shown-is-cut is the rule and its home. The reviewer's prompt is a person's change (D-079): if its statements need the rule, say so in the report rather than editing it.
- Primary (highlighted) button at the bottom right of every bottom bar.
- Chat-edit change marks only; direct edits show none.
- An epic's criteria are edited by hand in the Graph pane's node inspector as well as by the chat; only an epic's contract (and the contract after approval) shows its graph read-only.
- The plan is read against the spec only at each confirm (Confirm the plan, Confirm contract), and only where the spec or the criteria moved since the last reading; a plan the model drafted (Generate plan, Start over, Plan it again) counts as satisfying its spec, is never read as it lands and never lands on Problems, and Problems appears only after a confirm's reading finds a problem; no reading after a chat edit; problems are dismissed only at the command line, and only where nobody has edited the plan by hand since it was drafted (the chat's edits do not count); an open problem refuses the confirm for both shapes, with no way past; "Checking for drift" shows while a confirm's reading runs; Problems is the lowest tab.
- No chat dock on the contract or Problems tabs.
- The Watch page shows the agents' words live, no tool calls, latest at the bottom.
- Completed tickets stay on Home at the bottom with a check mark and Archive; nothing archives on its own; attention counts only until the ticket is opened.
- The founder's answers on cuts (wave 2, uncommitted, awaiting review), all under D-NEW-nothing-shown-is-cut:
  1. The executor's account (D-092) reaches only the next round's executor, so it keeps its cut with a marker: an excluded case.
  2. `perbo drift` redacts the report before measuring it, so a field redaction lengthens is asked for again in the same session; the desktop keeps a backstop that fails the reading with a message rather than cutting or dropping a field.
  3. No `text-overflow: ellipsis` or line clamp in the renderer: every such line wraps, the title bar and page header grow; `renderer/styles.test.ts` reads the shipped stylesheet and fails on either.
  4. Every capped typed field holds input at its limit from the schema's own constant (`TYPED_TEXT_MAX_CHARS` and its siblings in `shared/protocol.ts`); a box whose words go down composed with others (a lettered part in the chat, an own-words answer on the loop page) holds the room the rest leaves.
  5. The cut of a first turn names the folder only. "Untitled" is display only: the spec file carries no title line until the work is named (D-118).
  6. `packages/model` is left as the green commit has it; its transport-failure line is under Not built (SCP-188).
  7. `packages/review` is not edited: the round's edits there were never authorised and are removed, and the folder is byte-identical to `main`.
  8. A chat note about a tool's output (a parse reason, a stopped chat's stderr, a process error, a line that could not be recorded) says one sentence, with the output redacted and whole behind an "i" (`output` on the note).

## Merging with pull request #13
Round 8 removes every `.slice(0, 200)` from the write guard's refusal details; #13 keeps them and adds three (`backup.ts:58`, `command.ts:360`, `sed.ts:347` under `packages/runner/src/shell/internal/`). Strip them all in the merge and rerun `packages/runner/src/shell/index.whole-detail.test.ts`. #13 already fixes the two wrapped D-NEW ids in `apps/desktop/src/host/routes.ts`. The guard files, docs/08, D-105 and ADR-0038 changed on both branches.

## Before a local deploy
Check for runs in flight (`ps -axo pid,command | grep "perbo.js run"`); stopping the app terminates a running attempt. Package with `pnpm desktop:run`; stop the old app by pid; remove `Singleton*` in the profile directory; migrate `workspace.json` for new required fields with a `.bak` (`scripts/migrate-workspace-round8.mjs`, command above).
