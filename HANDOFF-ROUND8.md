# Handoff: round 8 of `home-loop-and-planning-care-package`

This file rides on the work-in-progress branch only. Delete it before anything here reaches the pull-request branch.

## Where things stand

- The pull-request branch `home-loop-and-planning-care-package` ends at `1e0de26` (the D-073 review's blocker fixes wait unpushed as `013d867` in a cloud fixer session, which this branch does not carry), gated (`pnpm check` code stages; the validators run under a Python with `yaml`). Its PR body is on the pull request.
- This branch holds that commit plus work-in-progress commits: six bundles built, reviewed and fixed; every fixer finished and reported. Every bundle has mutation-checked tests. The planning bundle (section 3) is fixed and independently reviewed, and that review's findings are fixed. The serial gate passes: install, build, runtime, corpus, validators (Python 3.11 with `yaml`), protected paths, the regression dry run, and every package's typecheck and lint. The test failures a root cloud container shows are its environment's and fail identically at `1e0de26`, which is green on GitHub: root ignores file modes and a set `GH_TOKEN` skips `gh auth status` (both pass as a non-root user without `GH_TOKEN`); `ssh-keygen` must be installed; `evaluation` `baseline.test.ts` and `runner` `loop/index.orphans.test.ts` fail there as any user, and `workspace` `diagnostic.ignored-paths.test.ts` times out only under the full parallel run. What remains: the founder's word to push the round's commit, and his local deploy with the profile migration: quit Perbo, then from the repository root `node scripts/migrate-workspace-round8.mjs ~/Library/Application\ Support/Perbo`. The second read of the Home, Watch-page and publish fixes is DONE and its findings are fixed (commit 6e77c31): Home and the review cards approved; the Watch page now carries whole turns with line breaks escaped on one physical line and no length cut; pull-request titles are whole sentences; every retained-publish refusal is tested and listed in D-NEW and `usage.ts`.
- Workflow the founder set: bundle by disjoint files → one Opus 5.5 builder per bundle → an independent Opus 5.5 reviewer per bundle (nothing lost, nothing beyond the ask, every test proven by mutation) → a fixer → a second read for large fixes → serial gate → ONE commit → local deploy on his Mac. No optimisation pass any more. No push or PR without his word. Commits carry `Assisted-by: LLM` and no `Co-Authored-By`. Never touch `packages/review/**` or `packages/runner/test/security.test.ts`; `git diff origin/main` on both must stay empty.

## Bundles and what is open

### 1. Continue after an interrupted run (runner `resume.ts`, `seal.ts`, `prompt.ts`, `loop/internal/{attempt,brief,state}.ts`, contracts `attempt.ts`)
Built and approved by two reviews. Retained diff taken with `--binary`; a diff that does not apply is dropped whole (reset to the sealed head) and the run continues; the diff is skipped where the branch already holds the sealed commit ("held"); the resumed executor's brief is truthful (`executor_resumed_v2`). Follow-ups noted, not fixed: a binary diff over 8 MiB leaves no `change.diff` and the refusal says "changed nothing"; the desktop's raw diff view shows base85 blocks; `packages/review/PROMPTS.md:22` says `executor_v11` (unprotected, one word).

### 2. Publish a retained branch later (`--publish-retained`; runner `loop/internal/retained.ts`, contracts `retained.ts`, CLI run command, desktop review/merge screens, D-NEW-publish-a-retained-branch-later)
Built, reviewed, fixed. "Next" always enabled on the review screen; the merge press publishes where no pull request exists. Declines now recorded at the seal (`ExecutionAttemptSchema.declines`); verification costs summed from `cv_` bundles; primary buttons at the bottom right; delivery record written under the run lock; preflight git and gh only. Fixed since: the `usage.ts` help lists every refusal; `retainedBranch` is read again under the run lock (`retained.ts:119`, CLI `run/index.ts:1431`); D-NEW's Built line records the known limit (a later run that declines without a new commit: the pull request lists the sealing run's declines). Second read done and its findings fixed (commit 6e77c31).

### 3. Planning flow for basic tickets (renderer `planning/**`, `panes.ts` `flowFor`, `SimpleTask.tsx`, `ContractPane.tsx`, `ContractScreen.tsx` `contractShows`, `CriteriaEditor.tsx`, `GraphPane.tsx`, `shared/{protocol,contract-editing}.ts`, host `plan/**`, D-NEW-basic-and-epic-flows, D-130 rewritten, `scripts/migrate-workspace-round8.mjs`)
DONE: review items 1–8 built, independently reviewed, and the review's findings fixed, each with a mutation-checked test. As built: an archived stopped ticket whose run job left the journal lands on the stopped page; the chat's edits to a basic ticket's criteria are marked on its contract (`EditingChange.by`), a hand edit marks nothing and clears them; a refused write says why and is not left writing; the last criterion cannot be deleted; `useDraftLanding` checks the impact of any opened planning with none recorded; an epic's criteria are read-only in the Graph node inspector (the chat edits them), no Plan pane; a basic ticket's drift reading runs at Confirm contract (button and ⇧⌘↵ alike) only where the spec or the criteria changed since the last reading, the Problems tab appears as for an epic, and the confirm is refused while any problem is open, including after a reading that could not be made, and while the drafts list does not yet carry the planning; resolving every problem takes the tab away and moves a person on it back to the contract; the "Asked N questions about …" line names whole titles, then "and K more", or the count alone; `scripts/migrate-workspace-round8.mjs` backs `workspace.json` up to `.bak`, brings every editing session up to the records, and refuses (exit 1, nothing written) a file that is missing or is not a profile (`version` 1, a `settings` object, a `repositories` list).
Open for the founder:
- (a) The Graph pane's "Node" button adds a placeholder criterion "New criterion: say what must be true." that, with the inspector read-only, only the chat can reword. Needs his ruling.
- (b) After Plan it again, the first confirm starts a drift job that the CLI answers from the draft's cached agreeing verdict (no model run), but it leaves a drift job in the journal. Likewise a re-read owed after resolving from the Problems page records no `read` state, so the next Approve starts a cached `driftCheck` job.
- (c) The basic-ticket problems hold lives in the renderer only: neither host refuses `run`/`approve` while problems are open.
- (d) No end-to-end test of a basic Plan it again reaching Impact.
- (e) Person-read cuts outside this round's files, not yet fixed under the no-cut ruling: `apps/desktop/src/host/interview/relay.ts` `readable()` `.slice(0, cap)` and the ended note `.slice(0, 2000)`; `apps/desktop/src/host/interview/host.ts:470` `.slice(0, 12_000)` (and `converse` refusing notes over 12,000 characters at `protocol.ts:69`); `packages/runner/src/account.ts:45`; `packages/runner/src/transport.ts:252,528`; `packages/runner/src/checks/index.ts:177`; `packages/runner/src/merge.ts:423`; `packages/runner/src/loop/internal/merge-up.ts:155`.
- (f) No decision in docs/11 states the no-cut rule yet.
Follow-up (not now): a basic re-draft drops Explorer marks (predates this bundle; epics too).

### 4. Live Watch page (contracts `spoken.ts`, runner `adapter.ts`, `codex/index.ts`, `loop/internal/{review,seal}.ts`, desktop `runner-progress.ts`, `task-context.ts` `watchTranscript`, `retained-output.ts`, OutputScreen in `ReviewScreens.tsx`)
Built, reviewed, fixed (22 mutations caught): the recorded list covers every attempt of the run (`useOutputs`, `watchTranscript` over `{attempt, transcript}` pairs); Codex subagent lines marked `subagent: true` and skipped; `logTail` drops the first partial line at both cut sites; Codex command lines folded (`oneLine` in contracts `spoken.ts`); reviewer lines redacted through `openFindingLines` with tests; deterministic-source findings print as the runner's own `finding:` line; stage-word regression test; "Continue the task" at the far right of the stopped page. Second read done and its findings fixed (commit 6e77c31).

### 5. Home (`HomePage.tsx`, `ticket-workspace.ts`, `shared/archive.ts`, `Rail.tsx`, `TaskPage.tsx`, sample host)
Built, reviewed, fixed: `ticketOpened` re-sent when the ticket's row becomes unseen while open; "completed" = decided only, greens "waiting on your merge decision" (badge, header pill, greeting, filters); decided last under every sort; `aria-describedby` on the card; lifecycle rows moved to contracts `ticket-transitions.ts` (root + browser) and the sample host's `moveTicket`/`reopenTicket` walk only legal rows, a stop writes no row. Second read done and its findings fixed (commit 6e77c31).

### 6. Review cards (`ReviewScreens.tsx` cards, `styles.css`, docs/15:32)
Built and approved; the badge and table words are to "directly verified", "inferred", "evidence not retained", "not reviewed" (card and table view; table column "Established").

## Founder's rulings today (apply everywhere)
- No caps on the COUNT of records Perbo writes; only a count cap a decision states by number stays (D-094, D-117, D-127, D-128). String-length caps STAY, with this rule: nothing is ever cut mid-sentence to fit; text that does not fit is condensed to fit. A model that writes the field is told the limit and asked to write concisely, and where its text overflows it is asked again to condense rather than clipped; a person typing into a field is held to the limit at the input; a line Perbo itself composes (progress, notes, PR sections) is written to fit in whole sentences. Any `.slice(0, n)` on text a person reads is a defect under this rule, except a log excerpt. The reviewer's prompt is a person's change (D-079): if its statements need the rule, say so in the report rather than editing it.
- Primary (highlighted) button at the bottom right of every bottom bar.
- Chat-edit change marks only; direct edits show none.
- No chat dock on the contract or Problems tabs.
- The Watch page shows the agents' words live, no tool calls, latest at the bottom.
- Completed tickets stay on Home at the bottom with a check mark and Archive; nothing archives on its own; attention counts only until the ticket is opened.

## Before a local deploy
Check for runs in flight (`ps -axo pid,command | grep "perbo.js run"`); stopping the app terminates a running attempt. Package with `pnpm desktop:run`; stop the old app by pid; remove `Singleton*` in the profile directory; migrate `workspace.json` for new required fields with a `.bak` (`scripts/migrate-workspace-round8.mjs`, command above).
