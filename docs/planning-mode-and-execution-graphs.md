# Planning mode and execution graphs

How large work goes through Perbo as one ticket. Built so far: Create in the rail, its picker and planning mode's Spec, Explorer and Graph panes, the Spec pane holding the spec and the Graph pane curating the graph and confirming it to the contract, where it is approved once, for a ticket it drafted and for one already in `plan_review`; the spec folder itself, with the slug its title takes and the title an untitled planning takes from the person's first turn ([D-118](11-open-decisions.md)), an id on every requirement, a generated page per node and a standing prohibition on the executor writing there, and the repository's standing prohibited list, which the explorer writes and the write guard reads; the plan's execution graph, the drafting of one from a spec, starting that draft over, the edit path that curates it and the size beside it ([D-100](11-open-decisions.md), [D-103](11-open-decisions.md), [D-104](11-open-decisions.md), [D-105](11-open-decisions.md)); the pinned checks and the review run once per node, narrowed to that node's paths, beside once over the whole change, with the gate reading the combination ([D-107](11-open-decisions.md)); the graph reading live while the work runs, each node's state and each criterion's derived from the sealed change set, those checks and the review artifact and from nothing the executor said of itself ([D-100](11-open-decisions.md)); the symbol and import index `perbo index` builds, and the Spec pane's `@Symbol` completion over it — the repository's exported names offered as a reference is typed, every reference marked, one the index does not hold marked apart with the nearest names offered in its place, and the names read by the host and by nothing a renderer sent ([D-015](11-open-decisions.md)); the Impact pane over the same index, which lists what a draft is likely to touch that its scope does not cover — once when the planning first has a plan, and by its button after that — and turns one warning into a scope mark or a No-Go ([D-015](11-open-decisions.md)); the stale-spec check over that index and over the spec's own bytes, which stops a run on a stale spec at `plan_invalid` and flags one already in flight ([D-103](11-open-decisions.md)); of `spec.md`'s three writers, the two panes' own saves never overwriting what they did not read — each says what it last read and a section both sides changed comes back refused with both texts rather than overwritten ([D-103](11-open-decisions.md)); the loop committing the spec folder, with the `CONTEXT.md` and ADR changes approval recorded, as the branch's first commit and reviewing the diff after it ([D-103](11-open-decisions.md)); and the interview itself, `perbo interview`, which runs the person's own Claude Code session through the Claude Agent SDK or their own Codex session through `codex app-server`, writes that spec's folder, `CONTEXT.md` and the ADR folder and refuses everything else, drafts the plan from the spec and changes it afterwards only through the validated edit path, docked in planning mode as a chat beside every pane, with its refusals shown as refusals, its plan edits shown with an undo, the plan's history over the pane and the conversation kept with the planning ([D-102](11-open-decisions.md)); and the executor starting subagents from the roles Perbo defines on both transports, each judged by the write guard from its own directory or thread and recorded against its role — on Codex, the role Codex reports for its thread — with review receiving none of it ([D-106](11-open-decisions.md)). The decisions are in [the register](11-open-decisions.md): [D-100](11-open-decisions.md), [D-101](11-open-decisions.md), [D-102](11-open-decisions.md), [D-103](11-open-decisions.md), [D-106](11-open-decisions.md), [D-104](11-open-decisions.md), [D-107](11-open-decisions.md) and [D-015](11-open-decisions.md). The architecture is in [ADR-0037](adr/0037-execution-graph.md) and [ADR-0038](adr/0038-subagents.md), the terms are in [CONTEXT.md](../CONTEXT.md), and a clickable prototype shows the layout on sample data, following the decisions; its parts and build script are in `design/prototypes/src/`. The work is in the private backlog ([D-076](11-open-decisions.md)); each entry's notes name the decision it builds.

## In one paragraph

Large work stays one ticket. Its plan may group its acceptance criteria into nodes, an execution graph, which a person curates and approves once, and the executor may hand parts of it to subagents of its own. The intent behind it is a spec, written in an interview between the person and their own agent session, kept in the repository and committed with the change. The desktop gains planning mode: Create moves into the rail and opens Spec, Explorer, Graph and Impact panes for one piece of work. A graph is checked and reviewed per node, and once over the whole change.

## The flow

1. **Create** (⌘1) opens planning mode, and the person picks the repository.
2. **Interview.** The person's own Claude Code or Codex session questions them in a chat docked beside every planning pane, and writes the spec, with any `CONTEXT.md` and ADR changes.
3. **Generate plan.** Pressed once in the chat. The session brings the spec up to date with the conversation, the drafter turns it into a contract and a suggested execution graph, and one ticket is admitted in `plan_review`.
4. **Refine by editing.** The person edits the graph by hand, marks paths in the explorer and reads impact warnings, and keeps talking in the chat. What they ask for there is applied to the plan and the spec as edits, each shown as a before-and-after they can undo. Nothing is generated again unless the person starts over from the spec, which drafts the same ticket again and drops the plan edits made since, keeping everything the spec holds.
5. **Approve** once.
6. **Run.** The loop commits the spec first, with the generated page for each node beside it. The executor works, starting subagents if it chooses, and receives its brief again after every compaction. The pinned checks run per node, narrowed to its paths, review runs per node and once overall, and the graph shows each node's state from the records.
7. **Deliver** as any ticket does: the pull request carries the spec with the change.

## Trust boundary

- **Data, not instruction.** The spec reaches the drafter delimited, as an issue does; `CONTEXT.md`, ADR titles and `principles.md` reach it as repository data ([D-035](11-open-decisions.md)). Impact warnings go to the person, not to a model.
- **Action parameters.** Node paths, prohibited paths and criteria become action parameters only through the person's approval ([D-072](11-open-decisions.md)). A path marked in the explorer is the person's own choice.
- **Deterministic precedence.** The write guard, the seal and the reviewer's deterministic scope check outrank any model claim. A node's state during a run comes from the sealed change set and review coverage, never from the executor's account.
- **The interview session** is the person's own, but every write it makes is decided by the runner's own write guard before it runs, which is why it loads no settings file, with that spec's own folder, `CONTEXT.md` and the ADR folder as the only places it may write and a list of read-only shapes bounding the commands the guard is asked about. Anything else is refused and said to the person rather than put to them as a question, and it cannot approve, publish or merge: there is no tool for any of the three.
- **Plan edits.** Before approval the interview changes the draft plan only through the validated edit path, which records its author, so the edit count still measures what a person changed.
- **Review inputs do not grow.** The spec commit sits below the reviewed diff, No-Gos stay in the approach, and subagent transcripts never reach review.

## Out of scope

- Interview providers beyond Claude Code and Codex.
- Phone surfaces for planning mode.
- Editing code in Perbo ([D-015](11-open-decisions.md)).
- Semantic end-to-end tests, database branching and just-in-time secrets; the environment contract ([ADR-0025](adr/0025-worktree-environment-contract.md)) owns that ground.
